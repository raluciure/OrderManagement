package dao;

import model.Client;

/**
 * ClientDAO is the class which extends AbstractDAO in order to make the methods implemented there being usable on
 * objects of Client type
 */
public class ClientDAO extends AbstractDAO<Client> {
}
